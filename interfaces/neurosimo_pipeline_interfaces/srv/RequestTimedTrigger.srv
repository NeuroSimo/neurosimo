# Used in the real-time pipeline for requesting timed trigger.
uint8[16] session_id
uint64 attempt_in_session

float64 trigger_offset          # timing offset relative to reference_sample_time
float64 reference_sample_time   # seconds since recording start
---
bool success
